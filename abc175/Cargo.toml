[package]
name = "abc175"
version = "0.1.0"
authors = ["guchio3 <astt.hwhw@gmail.com>"]
edition = "2018"

# dependencies added to new project
[dependencies]

[profile.release]
lto = true
panic = 'abort'
