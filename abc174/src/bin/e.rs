use proconio::{input, fastout};
use std::collections::BinaryHeap;

#[fastout]
fn main() {
    input! {
        n: usize, 
        k: usize, 
        a: [usize; n], 
    }
    let mut log_heap = BinaryHeap::<usize>::new();

    println!("{}", log_heap.);
}
